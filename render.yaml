services:
  - type: web
    name: finsight-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    # The current session cache is process-local; one worker keeps the demo
    # flow consistent while datasets remain durable in the database.
    startCommand: python init_db.py && gunicorn --workers 1 --threads 4 --timeout 120 app:app
    healthCheckPath: /healthz
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DB_HOST
        sync: false
      - key: DB_PORT
        value: "3306"
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: DB_NAME
        value: finsightai
      - key: DB_SSL_CA
        sync: false
      - key: MAX_CONTENT_LENGTH
        value: "16777216"
      - key: ALLOWED_EXTENSIONS
        value: csv,xlsx,xls,json
